apiVersion: v1
kind: Service
metadata:
  name: spring-backend
spec:
  type: NodePort
  selector:
    app: spring-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31234 # <-- You can pick a port between 30000-32767 or let K8s assign one
